# Стиль `<domain>`

Единый вид артефактов домена. Проект добавляет локальные правила
в своём `AGENTS.md`, не ослабляет этот файл молча.

## Именование

| Сущность | Форма | Пример-плейсхолдер |
|---|---|---|
| (контур) | | `<name>` |
| (файл проводки) | | |
| (пакет логики) | | |

## Комментарии

Только неочевидный intent. Без emoji и «# increment counter».
