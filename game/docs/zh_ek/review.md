# review.md — zh_ek (E6)

Итерация: `1` ревью реализации. Ревьюер не патчит этот diff.
Аналитика закрыта своим проходом. Секция analytics и строка C1 ниже сохранены.

## Вердикт

- Блокеров: `0`
- Выход разрешён: да
- Дедлок: нет

Цикл можно закрывать.

## Пункты

| ID | Суть (проблема / риск / пробел) | Приоритет | Статус | Итерация |
|---|---|---|---|---|
| C1 | Живые спеки слота и борды считали версию не `1` и не `2` порчей. Запись версии `3` тогда открывала новую игру вместо миграции | high | closed | 1 |
| R1 | Конечная точка в шести строках `text_for` не была в таблице спеки | low | closed | 1 |
| R2 | Смоук не фиксирует отдельно порчу чужого `notice_id` версии 3 и сумму урона с лидом. `go_run` id не стирает — это смоук проверяет | low | nit | 1 |

## Секция analytics

| ID | Суть | Блокер? | Статус |
|---|---|---|---|
| C1 | В дельте E6 файла `save_slot/spec.md` порча — версия не `1`, не `2` и не `3`. П.11 борды: целый файл версии 3 в ветку порчи не входит. Внешний контракт борды читает версии `1`, `2` и `3`. Ключи версии 3 — ключи версии 2 плюс `notice_id`. Чужое значение портит только версию 3. Лишний `notice_id` в файле 1 или 2 в памяти пустой. ZK3 проверяет и версию 1, и версию 2. П.9 борды: файл на диске остаётся прежней версии, в памяти версия текущей записи. Запечатывание пишет слот, чтобы новый `GameState` прочитал то же объявление. | да, закрыт | closed |

## Регрессия аналитики (итерация 1, после правки)

- Что перепроверили: шесть шаблонов и два эффекта, индекс `(last_floors + last_kills) % 3` на четырёх строках таблицы, эффект на следующем `go_run`, пустой `notice_id` без эффекта, одна строка стены без нового журнала, сложение урона и произведение скорости после лида, тексты без версии тайны Города.
- Новых вкусовых пунктов нет: да
- Новых блокеров правка не внесла: да

## Проверка реализации

Класс diff-а — `increment`: шесть шаблонов, два эффекта, `notice_id` в слоте версии 3, миграция целых файлов 1 и 2, смоук ZK1–ZK3. Отдельного журнала УК нет.

- `NoticeCatalog.pick` берёт пул выхода или смерти и индекс `(last_floors + last_kills) % 3`. Тяжесть — скорость ×0.90 у `bikes`, `elevator`, `boxes`. Бонус — +8 урона у `pot`, `bulb`, `seal`. Тексты совпадают с таблицей спеки.
- `_refresh_hub_meta` ставит id и строку и пишет слот. `go_run` вызывает `_apply_notice` и id не очищает. Пустой id даёт 0 и ×1.
- `apply_build` прибавляет бонус после лида и множит скорость после лида.
- `migrate` поднимает целые версии 1 и 2, лишний `notice_id` в них затирает пустой строкой, файл при чтении не пишет. Чужой id режет только версию 3.
- Стена — прежняя строка `zh_ek_notice`. `full_smoke` по-прежнему ищет «УК» и «Акт». Тексты не называют версию тайны Города.
- Смоук: до запечатывания 25/180, `elevator` и 162, повторное чтение слота, `pot` и 33, «Акт» и `seal`, `boxes` и 162, целые v1 и v2. Регрессия слота, этажей, боя, сборок, борды, полного прогона и петли зелёная.
