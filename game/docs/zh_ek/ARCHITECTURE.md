# ARCHITECTURE.md — zh_ek (E6)

| Модуль | Делает | Не делает |
|---|---|---|
| `game/scripts/notice_catalog.gd` | Шесть текстов и два эффекта по итогу | Не открывает сцену |
| `GameState` | В запечатывании ставит `notice_id`, строку стены и пишет слот | Не заводит второй журнал |
| `slot_store.gd` | Версия 3, миграция целых файлов 1 и 2 | Не считает их порчей |
| `player.apply_build` | Применяет тяжесть или бонус вместе с лидом | Не выбирает шаблон |

Строка по-прежнему видна в `apartment_hub` как `zh_ek_notice`.
