# review.md — combat_feel (E1)

Итерация: `2`. Критик не патчит `spec.md`, `assumptions.md`, `DESIGN.md`.

## Вердикт

- Блокеров: `1`
- Выход разрешён: нет
- Дедлок: нет

## Пункты

| ID | Суть (проблема / риск / пробел) | Приоритет | Статус | Итерация |
|---|---|---|---|---|
| C1 | Ранний Shift закрывает удар в спеке и в строке dodge, а `take_damage` в DESIGN молчит только до конца шага | high | blocker | 2 |
| C2 | Перезарядка 0.70 с с кадра нажатия, повтор во время шага игнорируется, удержание не продлевает неуязвимость | high | closed | 2 |
| C3 | Взгляд — последний ненулевой вектор движения, не кадр нажатия; до первого движения — вправо | high | closed | 2 |
| C4 | Срез — прежние три этажа, один тип врага на каждом, число этажей не меняется | high | closed | 2 |
| C5 | Откат врага 0.40 с: не бежит и не начинает новую подготовку | mid | closed | 2 |
| C6 | Попадание игрока белит визуал врага | low | closed | 2 |

## Секция analytics (если критик)

| ID | Суть | Блокер? | Статус |
|---|---|---|---|
| C1 | Спека п.5 и успех CF1: Shift в зоне во время подготовки закрывает удар этой подготовки, HP тот же, даже если шаг уже кончился и игрок всё ещё в зоне. A4 и строка dodge в DESIGN говорят то же: неуязвимость до этого удара, не дольше 0.50 с. Строка урона в DESIGN всё ещё: `take_damage` молчит по HP, пока шаг не кончился. Удар подготовки приходит в конце 0.50 с, шаг длится 0.16 с. Реализация строки урона снимает HP на раннем Shift при игроке в зоне и ломает успех CF1. Ошибка CF1 как раз называет этот исход. Пока строка урона и п.5 расходятся, путь нельзя и сдать, и отвергнуть. | да | open |
| C2 | Спека п.8, таблица, A4 и DESIGN: 0.70 с с кадра нажатия, повтор до конца перезарядки, включая шаг, игнорируется, удержание клавиши не продлевает неуязвимость. Вечный цикл шага снят. | нет | closed |
| C3 | Спека п.4 и таблица: смещение вдоль последнего ненулевого взгляда, взгляд не читается в кадр нажатия, до первого движения взгляд вправо. Это одна кнопка, не стик+кнопка. DESIGN не возвращает мышиный прицел и не читает стик в кадр `dodge`. | нет | closed |
| C4 | DESIGN: прежние три этажа, один тип врага на каждом, число этажей не меняется. Совпадает с границей спеки, CF5 и vertical slice. | нет | closed |
| C5 | Таблица спеки и DESIGN: откат 0.40 с, в откате враг не бежит и не начинает новую подготовку. Второй удар после отката определён. | нет | closed |
| C6 | П.7: попадание кратко белит визуал врага. На приёмку CF3 не влияло и сейчас с путём не спорит. | нет | closed |

## Регрессия (итерация ≥ 2)

- Что перепроверили: правки C1–C5 в `spec.md`, `assumptions.md`, `DESIGN.md`. Рядом: CF2 один удар 18 без касательного DPS; CF4 смерть через `take_damage` без закрытия; CF5 extract с последнего этажа и meta-boon не зависят от шага; одна кнопка `dodge`; окно закрытия не длиннее 0.50 с и не переживает следующий удар (откат 0.40 с, перезарядка с кадра нажатия).
- Новых вкусовых пунктов нет: да
- Новых id блокеров правка не внесла: да. C1 остаётся из-за строки урона в DESIGN.

## Вход

- spec / assumptions / дизайн: `game/docs/combat_feel/spec.md`, `assumptions.md`, `DESIGN.md`
- program E1: `docs/program/spec.md` (§ E1 — Бой, который читается)
- vertical slice: `game/docs/vertical_slice/spec.md` (death, extract, ≥3 этажа)
- test-report: нет (элаборация, до кода)
- доменный чеклист: итерация 2 — открытые пункты C1–C5 и регрессия
- GitHub PR: нет
