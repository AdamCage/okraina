# ARCHITECTURE.md — save_slot (E4)

| Модуль | Делает | Не делает |
|---|---|---|
| `game/scripts/slot_store.gd` | JSON, миграция версии 1, чтение и запись одного пути | Не меняет сцену |
| `GameState` | В `_ready` читает в свои поля. По закрытию окна пишет | Не хранит второй слот |
| `apartment_hub.gd` | Показывает поля, затем просит запись | Не выбирает путь файла |

Второй экземпляр скрипта `GameState` читает тот же файл в свои поля. Это проверка SV1, не второй слот игрока.
