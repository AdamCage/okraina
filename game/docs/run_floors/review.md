# review.md — run_floors (E3)

Итерация: `2`. Критик не патчит `spec.md`, `assumptions.md`, `DESIGN.md`, `ARCHITECTURE.md`.

## Вердикт

- Блокеров: `0`
- Выход разрешён: да
- Дедлок: нет

Код E3 начинать можно.

## Пункты

| ID | Суть (проблема / риск / пробел) | Приоритет | Статус | Итерация |
|---|---|---|---|---|
| C1 | Подготовка `meter` 0.85 с, окно шага E1 не длиннее 0.50 с. Ранний Shift в зоне к моменту удара имеет два обязательных исхода по HP | high | closed | 1 |
| C2 | У `meter` в таблице только «дистанция удара» 72. Не сказано, что это же число запускает подготовку и вооружает шаг. С дистанции 56–72 исход шага не определён | high | closed | 1 |
| C3 | `next_run_seed` без начального значения. Ноль — годный seed, у `int` старт 0, первый `go_run` процесса не берёт `randi()` | high | closed | 1 |
| C4 | Надпись жёлтой зоны: спека «ДАЛЬШЕ» / «ВЫХОД», DESIGN «дальше по дому» / «выход во двор». Место строки не назначено | mid | closed | 1 |
| C5 | Знаменатель этажа в HUD не назван. Прежний потолок 3 на длине 4–6 даёт ложную дробь | mid | closed | 1 |
| C6 | Старая дельта vertical slice требует различные этажи. Длина 6 по алгоритму повторяет id смещения, RF1 это не ловит | mid | closed | 1 |
| C7 | Точка прямоугольника не названа левым верхним углом. Числа `hall` сняты с текущего ColorRect | low | closed | 1 |

## Секция analytics

| ID | Суть | Блокер? | Статус |
|---|---|---|---|
| C1 | Подготовка `meter` — 0.85 с. Нажатие, когда до удара больше 0.50 с, удар не закрывает: игрок всё ещё в зоне 72 к моменту удара теряет 12 HP. Нажатие, когда до удара ≤ 0.50 с и игрок в зоне 72, закрывает удар: HP не меняется, даже если шаг уже кончился. Ранний шаг закрывать удар не обязан. `tenant`: 0.50 с, зона 56, урон 18. Окно E1 «не длиннее 0.50 с от нажатия» остаётся; второго обязательного исхода у раннего шага нет. | нет | closed |
| C2 | Строка таблицы называет у `meter` одним числом 72 дистанцию старта подготовки, зону удара и зону, в которой шаг закрывает удар. У `tenant` те же три роли остаются 56. Дистанция 56–72 — та же зона, не отдельное правило. | нет | closed |
| C3 | `next_run_seed` стартует с `-1`. Ноль — обычный seed, не «пусто»: `go_run` берёт поле при ≥ 0. | нет | closed |
| C4 | Подпись на самой жёлтой зоне — «ДАЛЬШЕ», на последнем этаже «ВЫХОД». Подсказка HUD отдельно — «Жёлтая зона — дальше по дому. Этаж k/n.» и на последнем «Последний этаж забега. Жёлтая зона — выход во двор / другой район.» Доля этажа — `k/run_length`. | нет | closed |
| C5 | Знаменатель доли этажа в подписи HUD — `run_length`, не бывший потолок 3. | нет | closed |
| C6 | Дельта E3 в `vertical_slice/spec.md`: старая дельта «≥3 различных этажей» не отменяет повтор id; при длине 6 алгоритм может повторить пресет смещения, когда пул зоны кончился. | нет | closed |
| C7 | Прямоугольник задан левым верхним углом и размером `w×h`. | нет | closed |

## Регрессия (итерация ≥ 2)

- Что перепроверили: C1 против E1 п.1, п.5 и строки «не дольше 0.50 с от нажатия»; C2 — три роли 72 против трёх ролей 56; C3 — старт `-1` и ноль как seed; C4 и C5 — виджет зоны, бытовая подсказка HUD и знаменатель `run_length`; C6 — строка дельты E3 в vertical slice при живой старой дельте «≥3 различных этажей»; C7 — якорь прямоугольника. DESIGN этажей совпадает с этими исходами: ранний шаг долгой подготовки удар не снимает, шаг в последние 0.50 с снимает, надпись зоны «ДАЛЬШЕ» / «ВЫХОД», подсказка говорит про дом и двор и номер из длины забега.
- Новых вкусовых пунктов нет: да
- Правка не добавила новых блокеров: да

## Вход

- spec / assumptions / дизайн: `game/docs/run_floors/spec.md`, `assumptions.md`, `DESIGN.md`, `ARCHITECTURE.md`
- program E3, планка и правила 5, 7, 8: `docs/program/spec.md`
- vertical slice, дельта E3 и старые буллеты: `game/docs/vertical_slice/spec.md`
- бой E1, шаг и телеграф: `game/docs/combat_feel/spec.md`, `DESIGN.md`
- находка на каждом этаже: `game/docs/run_builds/spec.md`
- lore §5, каркас гигахруща: `docs/world/LORE.md`
- инварианты: `game/AGENTS.md` — дискового save нет, IAP нет
- test-report: нет, до кода
- доменный чеклист реализации: не применялся
- GitHub PR: нет. Issue не требовался, A7 открыт, Issues API 403

Таблица сидов 1 и 2 принята как сверка с текстом `sequence`: длина 4 — `dryer, corridor, lift, pipes`; длина 5 — `hall, skipped, corridor, lift, switchboard`. Слоты «первый / середина / последний» на этих длинах из таблицы читаются, отдельным пунктом не вынесены. Генератор комнат, сохранение, босс и новое защитное действие в контракт не входят.
